// Configuration for Jinvani eBook Reader 
const CONFIG = { 
    // Media folder path (relative to index.html) 
    mediaFolder: 'media/', 
 
    // List of PDF files in the media folder 
    pdfFiles: [ 
    ], 
 
    // Application settings 
    settings: { 
        defaultZoom: 1.5, 
        enableKeyboardShortcuts: true, 
        showWelcomeScreen: true, 
    } 
}; 
